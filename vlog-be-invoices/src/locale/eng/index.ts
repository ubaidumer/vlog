import { Enum } from "./config.enum"
import { ConfigFields } from "./config.fields"
import { ResponsesConfig } from "./config.responses"


const ENG_LANG = {
   Enum,
   ConfigFields,
   ResponsesConfig

}

export {
    ENG_LANG
}
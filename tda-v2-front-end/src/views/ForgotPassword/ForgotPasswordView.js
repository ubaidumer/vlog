import React from 'react';
import { Hero } from './components';

const ForgotPasswordView = ({ themeMode }) => {
  return (
    <div>
      <Hero themeMode={themeMode} />
    </div>
  );
};

export default ForgotPasswordView;

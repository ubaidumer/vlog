import React from 'react';
import { Hero } from './components';

const ResetPasswordView = ({ themeMode }) => {
  return (
    <div>
      <Hero themeMode={themeMode} />
    </div>
  );
};

export default ResetPasswordView;
